opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
tensorflow>=2.13.0
pygame>=2.5.0
pyttsx3>=2.90
matplotlib>=3.7.0
pandas>=2.0.0
joblib>=1.3.0
# Phone-use distraction detection (YOLOv8n). Install the CPU torch/torchvision
# pair from https://download.pytorch.org/whl/cpu if you don't have a GPU.
ultralytics>=8.4.0
torch>=2.2.0
torchvision>=0.17.0
# Web hosting (web_app.py): FastAPI + uvicorn stream the annotated camera feed
# to the browser as MJPEG. Run with: python web_app.py  → http://localhost:8000
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
